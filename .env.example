# ===========================================
# Skadi Backend - Environment Variables
# ===========================================
# DESARROLLO LOCAL (valores por defecto si no se especifican)
# La app usa estos valores si no encuentra las variables de entorno

# --- PRODUCCIÓN (Railway) ---
# Establece estas variables en tu servidor/contenedor:

# Database (Railway MySQL)
DB_URL=jdbc:mysql://shuttle.proxy.rlwy.net:29762/railway?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=dkEDKMqPeajTynLiDilzydaYTltelkuf

# JWT (IMPORTANTE: Cambiar en producción)
JWT_SECRET=genera_una_clave_secreta_muy_larga_minimo_256_bits_para_produccion
JWT_EXPIRATION=86400000

# JPA / Hibernate (Producción)
JPA_DDL_AUTO=update
JPA_SHOW_SQL=false

# Server
SERVER_PORT=8080
